server {
    listen 80;
    listen [::]:80;
    server_name _;
    root /usr/share/nginx/html;

    # Don't add port or absolute URLs to redirects
    port_in_redirect off;
    absolute_redirect off;

    location / {
        try_files $uri $uri/index.html $uri/ =404;
    }
}
